<h1 class="noPadBottom">Eat The Burger?</h1>
<h1 class="noPadTop noPadBottom">Maybe you don't want to eat these burgers.</h1>
<h1 class="noPadTop noPadBottom">Think about it, for a while, a long while.</h1>

<div class="container notEaten">
    <h2>Burgers that you haven't eaten yet!</h2>

    <ul>
        {{#each burgers}}
        {{#unless devoured}}
        {{> burgers/burger-block devour=true}}
        {{/unless}}
        {{/each}}
    </ul>

    {{!-- <li>
        {{burgers_name}}

        <button class="change-devour" data-id="{{id}}" data-newdevour="{{devour}}">
            {{#if devoured}}Eat It{{else}}Delete It{{/if}}
        </button>

        <button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
    </li> --}}
</div>

<div class="container eaten">

    <h2>Burgers that have been eaten!</h2>

    <ul>
        {{#each burgers}}
        {{#if devoured}}
        {{> burgers/burger-block devour=false}}
        {{/if}}
        {{/each}}
    </ul>
</div>


<div class="container">
    <h2>Add a Burger</h2>
    <form class="create-form">

        <div class="form-group">
            <label for="bu">Burger Name:</label>
            <input type="text" id="bu" name="name">

        </div>

        {{!-- <div class="form-group">
        <label for="devour">Burgers you done ate</label><br>
        <input type="radio" name="sleepy" value="1" checked> Devoured!<br>
        <input type="radio" name="ready" value="0"> Ready to eat!<br>
    </div> --}}

        <button type="submit">Add Burger</button>
    </form>
</div>